# Receipt Wrangler MCP Server Configuration
# Copy this file to .env and fill in your values

# Receipt Wrangler instance URL
RECEIPT_WRANGLER_URL=http://localhost:8081

# Authentication credentials
RECEIPT_WRANGLER_USERNAME=your_username
RECEIPT_WRANGLER_PASSWORD=your_password

# Optional: Default group ID to use
RECEIPT_WRANGLER_GROUP_ID=your_default_group_id